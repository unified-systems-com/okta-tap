{
  "version": 1,
  "description": "okta CI record (req-boot-bootstrap-ci-record): the stack this plugin's own tests run in. Installs okta alone (it declares no dependencies); the consumer flips self to editable so the checkout under test runs, not the pinned revision. Population is empty: v0 ships no GRIFT. Deliberately excluded: every other plugin, all credentials, anything that reaches the network.",
  "install": {
    "plugins": [
      {
        "slug": "okta",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/okta-tap",
          "rev": "e1820f66a6e38b88bdc40f3b506f96ff9457af69"
        },
        "note": "Self, pinned at the bootstrap commit until the first tagged release; the consumer (plugin-ci, or `spawn --dev-plugins okta`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4)."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": []
  }
}
