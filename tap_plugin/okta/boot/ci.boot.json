{
  "version": 1,
  "description": "okta CI record (req-boot-bootstrap-ci-record): the stack this plugin's own tests run in. Installs the depends_on closure (identity_core, the vocabulary dependency behind SERVES_ISSUER__okta and HELD_BY_HUMAN__identity_core, from git at a pinned commit) and self; the consumer flips self to editable so the checkout under test runs, not the pinned revision. Population seeds the plugin's own GRIFT (the /okta page), which proves the bundle parses and its searches and panels resolve against a real registry. Installation fetches the two pinned git sources; nothing after installation reaches the network, and no credential is used. Deliberately excluded: every plugin outside the depends_on closure.",
  "install": {
    "plugins": [
      {
        "slug": "identity_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-identity-core",
          "rev": "53da388b6f47590090ef3bdc8d98731acff4c8e2"
        },
        "note": "Vocabulary dependency, pinned at the main commit carrying identity_core__human and HELD_BY_HUMAN__identity_core as well as identity_core__oidc_issuer (no tagged release carries the human yet; v0.1.2 predates it). Offline and credential-free."
      },
      {
        "slug": "okta",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/okta-tap",
          "rev": "e1820f66a6e38b88bdc40f3b506f96ff9457af69"
        },
        "note": "Self, pinned at the bootstrap commit until the first tagged release; the consumer (plugin-ci, or `spawn --dev-plugins okta`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4)."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": [
      {
        "type": "seed-plugin",
        "plugin": "okta",
        "enabled": true,
        "note": "Own GRIFT: the /okta page, its panels, searches, projection and layout."
      }
    ]
  }
}
